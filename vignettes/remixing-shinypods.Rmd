---
title: "Design of Shinypods"
author: "Ian Lyttle & Alex Shum"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Design of Shinypods}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

In this vignette, we go into a little more detail of the structure of a shinypod.

## Essentials

There are four essential functions: three for the UI and one for the server. Let's consider the dygraph shinypod:

- `dygraph_ui_input()` returns a named `shiny::tagList` of input elements
- `dygraph_ui_output()` returns a named `shiny::tagList` of output elements
- `dygraph_ui_misc()` returns a named `shiny::tagList` of miscellaneous elements, like help guides.
- `dygrpah_server()` contains the server logic

### Convention

The functions `dygraph_sidebar_side()` and `dygraph_sidebar_main()` draw upon the elemental ui functions: `dygraph_ui_input()`, `dygraph_ui_output()` and `dygraph_ui_misc()`.s


## Presentation layer



